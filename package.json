{
  "name": "marketpulse-ai-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root workspace management scripts for MarketPulse AI",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start"
  }
}
